// 4. Badge colors (NOT stored in DB)
export const badgeClasses = {
  sale: "bg-danger",
  hot: "bg-warning",
  promo: "bg-success",
  new: "bg-primary",
  none: "d-none", // hide when no badge
};

// 3. Static colors for promo cards (NOT stored in DB)
export const promoColors = {
  1: "bg-info",
  2: "bg-primary",
  3: "bg-secondary",
};
